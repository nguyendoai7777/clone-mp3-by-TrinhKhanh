import * as Types from './../Constants/ActionTypes';
var initialState = {
    
}
    
const Ui = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return { ...state };
    }
};
export default Ui;
